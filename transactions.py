# add_transaction(user_id, type, amount, category, date, desc, payment_method)
# view_transactions(user_id)
# edit_transaction(transaction_id, new_values)
# delete_transaction(transaction_id, confirm=True)
